# SPDX-License-Identifier: Apache-2.0
# Copyright 2024 Intel Corporation
name: linters
on:
  push:
    branches:
      - master
  pull_request:

jobs:
  buflint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: buflint
        env:
          BUF_VERSION: 1.0.0
        run: |
          cd ..
          sudo git clone https://github.com/onosproject/onos-lib-go.git
          cd onos-e2-sm
          docker run \
            -v `pwd`:/go/src/github.com/onosproject/onos-e2-sm \
            -v `pwd`/../onos-lib-go/api/asn1:/go/src/github.com/onosproject/onos-e2-sm/servicemodels/asn1 \
            -w /go/src/github.com/onosproject/onos-e2-sm/servicemodels \
            bufbuild/buf:${BUF_VERSION} lint

  sm-linters-e2sm_kpm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_kpm && golangci-lint run --timeout 5m

  sm-linters-e2sm_rc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_rc && golangci-lint run --timeout 5m

  sm-linters-e2sm_kpm_v2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_kpm_v2 && golangci-lint run --timeout 5m

  sm-linters-e2sm_kpm_v2_go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_kpm_v2_go && golangci-lint run --timeout 5m

  sm-linters-e2sm_ni:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_ni && golangci-lint run --timeout 5m

  sm-linters-e2sm_rc_pre:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_rc_pre && golangci-lint run --timeout 5m

  sm-linters-e2sm_rc_pre_go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_rc_pre_go && golangci-lint run --timeout 5m

  sm-linters-e2sm_mho:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_mho && golangci-lint run --timeout 5m

  sm-linters-e2sm_rsm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/e2sm_rsm && golangci-lint run --timeout 5m

  sm-linters-test_sm_aper_go_lib:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd servicemodels/test_sm_aper_go_lib && golangci-lint run --timeout 5m

  sm-linters-protoc-gen-cgo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd protoc-gen-cgo/ && golangci-lint run --timeout 5m

  sm-linters-protoc-gen-choice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd protoc-gen-choice/ && golangci-lint run --timeout 5m

  sm-linters-protoc-gen-builder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: golangci/golangci-lint-action@v3
      - uses: actions/setup-go@v5
        with:
          go-version-file: 'go.mod'
      - name: sm-linters
        run: |
          cd protoc-gen-builder/ && golangci-lint run --timeout 5m
